# RAG Project (CV Demo)

This repository is a small Retrieval-Augmented Generation (RAG) prototype. It includes the core scripts for document processing, indexing, and retrieval, plus a tiny demo to run locally without large data or GPU.
